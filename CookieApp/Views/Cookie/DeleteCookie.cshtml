@{
    ViewBag.Title = "Delete Cookie";
}

<div class="container mx-auto mt-10">
    <h2 class="text-2xl font-bold mb-6">Cookie Deleted</h2>
    <p>The cookie has been deleted.</p>
    <p class="mt-4">
        <a href="@Url.Action("Index", "Cookie")" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">Go back</a>
    </p>
</div>
